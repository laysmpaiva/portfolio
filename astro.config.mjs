// @ts-check
import { defineConfig } from 'astro/config';

// Deployed as a GitHub user site → served at the root domain.
// https://astro.build/config
export default defineConfig({
  site: 'https://laysmpaivatfm.github.io',
});
